# Educator demo mode (npm run dev:educator-demo) — fixtures only, never a real project.
# Overrides .env so the demo never touches Firebase; see README "Educator demo mode".
VITE_PERSISTENCE=local
VITE_EDUCATOR_DEMO=1
